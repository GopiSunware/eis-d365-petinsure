# ===========================================
# WS8 Admin Portal - Environment Configuration
# ===========================================
# Copy this file to:
#   - backend/.env (for backend configuration)
#   - frontend/.env.local (only NEXT_PUBLIC_* and PORT needed)
# ===========================================

# ===========================================
# PORT CONFIGURATION
# ===========================================
BACKEND_PORT=8008
FRONTEND_PORT=8081

# Frontend environment variable (for .env.local)
NEXT_PUBLIC_API_URL=http://localhost:8008

# ===========================================
# MODE CONFIGURATION
# ===========================================
DEBUG=True
DEV_MODE=True

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Leave empty for auto-generation in DEBUG mode
# For production, set explicit origins (comma-separated):
# CORS_ORIGINS=https://admin.example.com,https://app.example.com

# ===========================================
# AUTHENTICATION
# ===========================================
JWT_SECRET_KEY=change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Azure AD (Optional - for SSO)
# AZURE_AD_TENANT_ID=
# AZURE_AD_CLIENT_ID=
# AZURE_AD_CLIENT_SECRET=

# ===========================================
# DATABASE (Optional - falls back to in-memory)
# ===========================================
# COSMOS_DB_ENDPOINT=https://your-cosmos.documents.azure.com:443/
# COSMOS_DB_KEY=your-key
# COSMOS_DB_DATABASE=admin-portal

# ===========================================
# AZURE COST MANAGEMENT
# ===========================================
# Required for Azure cost data
# Uses DefaultAzureCredential (az login locally, managed identity in cloud)
AZURE_SUBSCRIPTION_ID=your-subscription-id

# Optional: Filter costs by resource group name patterns (comma-separated)
# Only resource groups containing these strings will be included
# AZURE_RESOURCE_GROUP_FILTER=eis,dynamics,prod

# ===========================================
# AWS COST EXPLORER
# ===========================================
# Required for AWS cost data
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1

# Optional: Filter costs by AWS service names (comma-separated)
# Supports short names: Bedrock, Lambda, S3, API Gateway, CloudWatch, etc.
# AWS_SERVICE_FILTER=Bedrock,Lambda,S3,API Gateway,CloudWatch
